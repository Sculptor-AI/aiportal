# Hosted backend used when "Use real backend" is enabled
VITE_REMOTE_BACKEND_URL=https://api.sculptorai.org

# Local backend used by the Vite /api proxy when "Use local proxy" is enabled
VITE_LOCAL_BACKEND_PROXY_TARGET=http://localhost:8787

# Features
VITE_ENABLE_SEARCH=true
